# Barcode Reader App

This project contains a simple React Native app built with Expo that scans barcodes using the device camera.

## Development

Install dependencies and start the development server:

```
npm install
npx expo start
```

## Testing

Run tests with:

```
npm test
```
